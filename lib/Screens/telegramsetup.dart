import 'package:flutter/material.dart';

class Telegramsetup extends StatelessWidget {
  const Telegramsetup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}